# activation caches / model weights — never commit
caches/
artifacts/

# run outputs: keep the result JSONs, drop the rest
runs/*
!runs/*.json
*.npy
*.memmap
*.safetensors
*.bin

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# local config / secrets
.env
.env.*

# phase 0 scratch
paper.txt
